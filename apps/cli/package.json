{
  "name": "@factory/cli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "bin": {
    "factory": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "bun test",
    "build": "bun build --compile --outfile dist/factory ./src/index.ts"
  }
}
