// Build-time constants injected by Vite via `define` (see vite.config.ts).
// Treated as string literals at compile time, so they can be used wherever
// a normal string would.
declare const __FACTORY_VERSION__: string;
