---
id: task-017
title: Claude-surface parity inventory + parity contract
status: ready
priority: med
estimate: small
created: 2026-05-25T17:43:24.851Z
updated: 2026-05-25T17:43:24.851Z
labels:
  - feature-plan-task
---

## Acceptance

- [ ] A checked-in document (e.g. `docs/internal/codex-parity.md`) enumerating **every** claude invocation site in the codebase — at minimum: `packages/runtime/src/agents/claude/*`, `runtime.spawn` callers, `apps/daemon/src/triage/orchestrate.ts`, `apps/daemon/src/plans/{iterate,refine,bootstrap-from-plan,apply-feature-plan,apply-project-vision}.ts`, `apps/daemon/src/audits/{iterate,exec-iterate}.ts`, and the feedback iterate path
- [ ] For each site: the prompt shape it sends, the output contract it parses (factory-status footer, fenced JSON envelope, plain text, etc.), and an explicit codex-parity acceptance test ("site X with agent=codex produces the same downstream effect as agent=claude")
- [ ] Inventory is generated by reading the code, not by recollection — every entry cites file:line
- [ ] Every site in this inventory is either marked `parity-verified` (with a passing test) or `parity-blocked` (with a written gating reason + follow-up plan reference). No site stays in an indeterminate state.

## Notes

Emitted by feature plan nt7386gu: "Full support for OpenAI Codex as an agent harness/model powering Factory/Heimdall. We could use either headless codex cli, or the codex sdk as long as it supports use of subscription usage and not just api key."

